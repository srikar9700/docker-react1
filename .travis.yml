sudo: required
services:
  - docker

before_install:
  - docker build -t srikar9700/docker-react1 -f Dockerfile.dev .

script:
  - docker run -e CI=true srikar9700/docker-react1 npm run test

